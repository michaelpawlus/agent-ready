# perfect

A fixture project that hits every agent-readiness check.

## Agent-Friendly Interface

This CLI is designed for agents -- every command takes `--json` and runs
non-interactively by default.
